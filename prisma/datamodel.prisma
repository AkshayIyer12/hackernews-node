type Link {
     id: ID! @unique
     postedBy: User
     description: String!
     url: String!
}

type User {
     id: ID! @unique
     name: String!
     email: String! @unique
     password: String!
     links: [Link!]!
 }
